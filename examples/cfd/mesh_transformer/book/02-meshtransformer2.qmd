# ISLA: an invariant slice-attention surrogate {#sec-mt2}

ISLA (Invariant Slice Attention; the code class is `ISLA`, formerly `MeshTransformer2`) is a soft-slice transformer whose backbone never
sees a coordinate. Every learned operation acts on rotation- and
translation-invariant scalars built from the surface geometry and the
freestream direction, and vector outputs are assembled at the very end
from an equivariant basis. The result is a surrogate whose rotation and
translation covariance is exact to floating-point roundoff rather than
learned, whose parameter count matches GeoTransolver's (8.9M vs 9.0M)
and whose peak training memory at 10,000 tokens is 1.5 GB (GeoTransolver
4.6 GB), and whose accuracy advantage
on small training sets (@sec-data-efficiency) comes from the combination
of its parts rather than from any one of them (@sec-mechanism). This chapter
describes what ISLA computes, states its contracts as the tests that
verify them, and prices each design choice with the measurements from
the results chapters.

ISLA pays for symmetry once at the network's edges (invariant inputs,
equivariant output basis) and spends the interior on the routing
mechanism that has the strongest empirical record on industrial
aerodynamics, the soft-slice attention of the Transolver family
(@sec-baselines). The opposite trade, enforcing physical structure inside
every layer with typed scalar/vector streams and analytic
boundary-integral kernels, fails on separated 3D flow
(@sec-interior-negatives).

## What goes in and what comes out {#sec-mt2-architecture}

**ISLA maps a sampled surface plus a freestream direction to fields at
query points, and every field it produces transforms with the input
under rigid motions.** The inputs, per case, are:

- a set of $N$ sampled surface points $x_i \in \mathbb{R}^3$ (10,000 per
  case in the protocol of @sec-protocol);
- the unit outward normal $n_i$ at each point;
- a measure weight $w_i > 0$ per point: the surface area the point
  stands for, so that $\sum_i w_i f(x_i)$ approximates $\int f \, dA$;
- one global drive vector $d \in \mathbb{R}^3$, the freestream direction.

The outputs are one or more scalar fields (surface pressure in every
result in this book) and one or more vector fields (wall shear stress)
at query points. In the reference configuration the queries are the
surface sample itself and each point receives its own prediction. With
the passive decoder enabled (`query_independent=True`), queries may be
any positions, on or off the surface, and the prediction at one query
does not depend on which other queries are evaluated.

Three properties hold by construction, independent of training:

1. Rotating and translating the inputs rotates the vector outputs by the
   same rotation and leaves the scalars unchanged.
2. Multiplying every measure weight by the same constant changes nothing.
3. In the passive-decoder mode, the prediction at a query is a function
   of the surface sample and that query alone.

A fourth property, invariance to a uniform geometric rescaling of the
surface (points scaled by $k$, areas by $k^2$), holds when the
similarity gauge is enabled (`similarity_gauge=True`) and not
otherwise; the default reference length is a constant.
@sec-mt2-contracts states each property as the test that verifies it.

```{mermaid}
%%| fig-cap: >
%%|   The ISLA forward pass. Blue stages are learned; grey boxes
%%|   are data. The backbone (gauge through the last slice layer) consumes
%%|   only invariant scalars; the only place a vector is reconstructed is
%%|   the output basis. The passive decoder on the right is optional: in the
%%|   reference configuration the surface tokens themselves are the queries
%%|   and the heads read the encoder state directly. Not shown: the
%%|   per-layer residual MLPs and layer norms, and the optional flags of
%%|   @sec-mt2-design.
flowchart TB
    classDef data fill:#f4f3ee,stroke:#898781,color:#0b0b0b
    classDef learned fill:#e6f0fb,stroke:#2a78d6,color:#0b0b0b
    classDef fixed fill:#ffffff,stroke:#2a78d6,stroke-dasharray:4 3,color:#0b0b0b
    classDef out fill:#e6f0fb,stroke:#2a78d6,stroke-width:2px,color:#0b0b0b

    A["surface sample<br/>positions x_i, normals n_i, areas w_i<br/>freestream direction d"]:::data
    B["gauge<br/>centre c and reference length L<br/>r_i = (x_i - c) / L"]:::fixed
    C["invariant seeds<br/>|r_i|, log|r_i|, r_i.d, r_i.n_i, n_i.d"]:::fixed
    D["embed MLP -> h_i"]:::learned
    E["slice layer x 12<br/>measure-weighted routing a_is<br/>anchors z_s, m_s<br/>8 relational invariants g_is<br/>slice states Z_s, read-back"]:::learned
    F["encoder state<br/>h_i, final slices Z_s, anchors z_s, m_s"]:::data
    Q["query points x_q (and normals n_q)"]:::data
    R["passive read block x 4<br/>read slices, anchors, local kernel<br/>never write back"]:::learned
    H["heads<br/>scalars; 7 coefficients per vector"]:::learned
    V["equivariant basis<br/>d, n, r, e_theta(r,n), e_phi(r,n), e_theta(r,d), e_phi(r,d)"]:::fixed
    O["scalar and vector fields at queries"]:::out

    A --> B --> C --> D --> E --> F
    F --> H
    F -. optional .-> R
    Q -.-> R
    R -.-> H
    H --> O
    V --> O
```

## The forward pass {#sec-mt2-forward}

**Every stage below is either a fixed geometric map or a learned map
acting on invariants; the two never mix until the output basis.** The
symbols are tied to the identifiers in
`physicsnemo/experimental/nn/isla/model.py`.

| symbol | code | meaning |
|---|---|---|
| $x_i, n_i, w_i, d$ | `points`, `normals`, `measure_weights`, `drive` | inputs: positions, unit normals, cell areas, freestream direction |
| $c, L$ | `center`, `gauge` (or `reference_length`) | gauge: centre and reference length that nondimensionalize positions |
| $r_i, \hat r_i, \hat n_i, \hat d$ | `r`, `r_hat`, `n_hat`, `d_hat` | scaled relative position, and unit directions of position, normal, drive |
| $\ell_i$ | `log_w` | log measure weight, the bias that makes routing quadrature-weighted |
| $s_i$ | `invariants` | the five invariant seeds per point |
| $h_i$ | `h` | hidden state per point, width 192 |
| $\lambda_{is}$ | `logits` | routing logit of point $i$ to slice $s$; $S = 256$ slices |
| $a_{is}$ | `a` | routing weight, normalized over points for each slice |
| $z_s, m_s$ | `z_pos`, `m_s` | equivariant anchors: weighted mean position and mean normal of slice $s$ |
| $g_{is}$ | `geo` | the eight relational invariants of point $i$ against anchor $s$ |
| $Z_s$ | `z` | slice state: the measure-weighted mean of $h$ over the slice |
| $\pi_{is}$ | `point_mix` | read-back weight, normalized over slices for each point |
| $b_i, \gamma_i$ | `back`, `geo_pool` | what a point reads back: slice states and pooled relational invariants |
| $e_\theta, e_\phi$ | `spherical_basis` | tangential basis vectors completing $\{\hat d, \hat n, \hat r\}$ |

### Gauge

The gauge fixes the origin and the unit of length. With the default
constant gauge,

$$
c = \frac{1}{N}\sum_i x_i, \qquad L = L_{\mathrm{ref}} = 8.0,
$$

where $L_{\mathrm{ref}}$ is the `reference_length`. The recipe
nondimensionalizes every coordinate by the dataset's per-case reference
length before the model sees it (5 m on DrivAerML), so the constant 8.0
is in those nondimensional units: 40 m physical on DrivAerML. With the
similarity gauge on,

$$
\tilde w_i = \frac{w_i}{\sum_j w_j}, \qquad
c = \sum_i \tilde w_i\, x_i, \qquad
L = \Big(\sum_i \tilde w_i\, \lvert x_i - c\rvert^2\Big)^{1/2},
$$

the measure-weighted centroid and the measure-weighted RMS radius. In
both cases $r_i = (x_i - c)/L$, $\hat r_i = r_i/\lvert r_i\rvert$,
$\hat n_i = n_i/\lvert n_i\rvert$ and $\hat d = d/\lvert d\rvert$. The
constant gauge makes $\lvert r_i\rvert$ an absolute size, so the network
sees how large the body is; the measure-weighted gauge removes that
information and, because it is an integral rather than a point average,
does not move when the sampling density is redistributed over the
surface. The first is the configuration behind every trained checkpoint
in the results chapters; the second is the configuration for
density-robust deployment (@sec-robustness-density).

### Invariant seeds

Each point enters the backbone as five scalars:

$$
s_i = \big[\, \lvert r_i\rvert,\ \log\lvert r_i\rvert,\
\hat r_i\!\cdot\!\hat d,\ \hat r_i\!\cdot\!\hat n_i,\ \hat n_i\!\cdot\!\hat d \,\big],
\qquad h_i^{(0)} = \mathrm{MLP}_{\mathrm{embed}}(s_i).
$$

These are the complete rotation invariants of the triple
$\{r_i, \hat n_i, \hat d\}$ up to parity (norms and dot products; no
triple product). They locate a point radially and orient it relative to
the drive and to its own normal; they do not, by themselves, separate
two points at the same radius with the same orientation. Separation is
the job of the slice layers, which supply anchors to measure against.
The measure weight enters as a log-space bias, $\ell_i = \log w_i$ (zero
if weights are absent or `use_measure_weights=False`).

### Measure-weighted soft-slice routing

Each of the 12 pre-LayerNorm slice layers (`_SliceBlock`) routes every
point to every slice with a softmax over *points*:

$$
\lambda_{is} = W_a\, \mathrm{LN}(h_i), \qquad
a_{is} = \frac{\exp(\lambda_{is} + \ell_i)}{\sum_j \exp(\lambda_{js} + \ell_j)}.
$$

The bias $\ell_i$ turns each slice's aggregate into a quadrature rule:
$\sum_i a_{is} f_i$ is a weighted mean whose weights are proportional to
$w_i$ times a learned, content-dependent factor. Adding a constant to
every $\ell_i$ cancels in the ratio, which is the measure-weight scale
invariance verified in @sec-mt2-contracts.

### Equivariant anchors

From the routing weights each slice forms two vectors,

$$
z_s = \sum_i a_{is}\, r_i, \qquad
m_s = \frac{\sum_i a_{is}\, \hat n_i}{\big\lvert \sum_i a_{is}\, \hat n_i \big\rvert},
$$

a weighted mean position and a mean normal direction. Both are linear
in equivariant inputs with invariant weights, so they rotate with the
body. They give the layer 256 data-adaptive reference points; a point's
position is then described by its relation to this constellation
rather than by coordinates in any frame.

### Relational invariants, routing refinement and read-back

For every point–anchor pair, with $\rho_{is} = r_i - z_s$,
$\hat\rho_{is} = \rho_{is}/\lvert\rho_{is}\rvert$ and
$\hat z_s = z_s/\lvert z_s\rvert$, the layer forms eight invariants:

$$
g_{is} = \big[\,
\lvert\rho_{is}\rvert,\ \log\lvert\rho_{is}\rvert,\
\hat\rho_{is}\!\cdot\!\hat d,\ \hat\rho_{is}\!\cdot\!\hat n_i,\
\hat\rho_{is}\!\cdot\! m_s,\ \hat n_i\!\cdot\! m_s,\
\lvert z_s\rvert,\ \hat z_s\!\cdot\!\hat d
\,\big].
$$

Six are genuinely relational (distance to the anchor and the direction
cosines of the separation against the drive, the point's normal and the
anchor's normal, plus the alignment of the two normals); the last two
describe the anchor itself and are broadcast to every point. A linear
map of $g_{is}$ to one scalar refines the routing,
$\lambda'_{is} = \lambda_{is} + w_g\!\cdot g_{is} + b_g$, and the routing
weights are recomputed with the same measure-weighted softmax. The slice
state is the routed mean of the hidden states, passed through a
per-slice MLP:

$$
Z_s = \sum_i a'_{is}\, h_i, \qquad Z_s \leftarrow Z_s + \mathrm{MLP}_{\mathrm{slice}}(\mathrm{LN}(Z_s)).
$$

Reading back uses a softmax over *slices* (no measure bias, since a
point is choosing where to read, not how much it weighs):

$$
\pi_{is} = \frac{\exp(\lambda'_{is})}{\sum_t \exp(\lambda'_{it})}, \qquad
b_i = \sum_s \pi_{is}\, Z_s, \qquad
\gamma_i = W_f \Big(\sum_s \pi_{is}\, g_{is}\Big) + b_f,
$$

$$
h_i \leftarrow h_i + W_b\,[\,h_i;\ b_i;\ \gamma_i\,], \qquad
h_i \leftarrow h_i + \mathrm{MLP}(\mathrm{LN}(h_i)).
$$

The order of operations in $\gamma_i$, pool the eight invariants over
slices and then project to width 96, is what sets ISLA's memory footprint
(@sec-mt2-cost). The point ends the layer knowing
which slices it belongs to, what those slices contain, and where it sits
relative to their anchors.

### Passive decoder

With `query_independent=True`, the encoder above runs unchanged on the
surface sample and its final state is summarized once: a final
measure-weighted routing gives slice states $Z_s$ and anchors
$z_s, m_s$ from the last hidden states. Each query $q$ with position
$x_q$ and normal $n_q$ is placed in the same gauge, $r_q = (x_q - c)/L$,
seeded with the same five invariants, and passed through four read
blocks (`_ReadBlock`). A read block forms the eight relational
invariants $g_{qs}$ against the final anchors, routes over slices,

$$
\pi_{qs} = \mathrm{softmax}_s\big(W_a\,\mathrm{LN}(h_q) + w_g\!\cdot g_{qs}\big), \qquad
h_q \leftarrow h_q + W_b\,[\,h_q;\ \textstyle\sum_s \pi_{qs} Z_s;\ W_f \sum_s \pi_{qs}\, g_{qs} + b_f\,],
$$

and then reads the encoder states near the query with a
measure-weighted Gaussian kernel of width $\varrho$ (`local_readout_rho`,
0.02 gauge units),

$$
u_q = \frac{\sum_i w_i\, e^{-\lvert r_q - r_i\rvert^2/\varrho^2}\, h_i}
{\sum_i w_i\, e^{-\lvert r_q - r_i\rvert^2/\varrho^2}}, \qquad
h_q \leftarrow h_q + W_l\,[\,h_q;\ u_q\,],
$$

followed by the residual MLP. Nothing in a read block writes into
$h_i$ or $Z_s$, so the map from encoder state to $h_q$ is a function of
$x_q$, $n_q$ and the source alone. For queries off the surface
(`interior_queries=True`, @sec-interior) a normal is synthesized
equivariantly as the soft assignment of $r_q$ to the anchors
$z_s$ applied to the anchor normals $m_s$; explicitly supplied
`query_normals` override it.

### Scalar and vector heads

A single linear head on the normalized final state produces the scalar
outputs and, for each vector output, seven coefficients:

$$
[\,p_i;\ c_{i}\,] = W_h\, \mathrm{LN}(h_i), \qquad
v_i = \sum_{k=1}^{7} c_{ik}\, B_{ik}, \qquad
B_i = \big\{\hat d,\ \hat n_i,\ \hat r_i,\
e_\theta(\hat r_i, \hat n_i),\ e_\phi(\hat r_i, \hat n_i),\
e_\theta(\hat r_i, \hat d),\ e_\phi(\hat r_i, \hat d)\big\}.
$$

Here $e_\theta(\hat r, \hat a)$ is the component of $-\hat a$
orthogonal to $\hat r$ and $e_\phi = \hat r \times e_\theta$, both left
unnormalized so that they vanish smoothly where $\hat r \parallel \hat a$
and no direction field on the sphere has to be combed. The three input
directions already span $\mathbb{R}^3$ generically; the four complements
make the expansion well conditioned when they are nearly coplanar. All
coefficients are invariants and all basis vectors are covariant, so
$v_i$ rotates with the input. Finally the whole output is multiplied by
$\lvert d\rvert$; the recipe supplies a unit direction, so this factor is
identically one and carries no physical content.

## Contracts, as tests {#sec-mt2-contracts}

**Each contract is a property of the code, verified in double precision
on random weights and a random anisotropic point cloud, not a property
of a trained checkpoint.** The tests live in
`test/experimental/nn/test_isla.py` (33 tests). The fixture is a
3-layer, 32-slice, width-64 model in fp64 on 500 points with random
normals, drive and weights; tolerances are absolute.

| test | assertion | tolerance |
|---|---|---|
| `test_rotation_equivariance` | for a random proper rotation $Q$: scalars unchanged, vectors $v \mapsto vQ^\top$ | $10^{-10}$ |
| `test_translation_invariance` | shifting all points by a constant leaves every output unchanged | $10^{-10}$ |
| `test_measure_weight_scale_invariance` | multiplying all $w_i$ by 137 leaves every output unchanged | $10^{-9}$ |
| `test_drive_degree_one` ($k \in \{0.5, 2, 4\}$) | scaling $d$ by $k$ scales every output by $k$ | $10^{-10}$ |
| `test_collated_input_shapes` | drive as $(B,1,3)$ or $(3,)$ gives the same output as $(B,3)$ | $10^{-12}$ |
| `test_query_set_independence` | passive decoder: same source, 50 queries alone vs the same 50 plus 100 others; the shared predictions agree | $10^{-12}$, rtol 0 |
| `test_qi_rotation_equivariance`, `test_qi_drive_degree_one` | rotation and drive contracts hold in the passive-decoder configuration | $10^{-10}$ |
| `test_anchor_conditioned_decode_query_independence` | with a fixed interacting anchor subset (`n_anchors`), query independence and rotation covariance both hold | $10^{-12}$ / $10^{-10}$ |
| `test_interior_queries_contracts` | off-surface queries with no normals: rotation covariance, translation invariance, query independence, finite outputs | $10^{-10}$ / $10^{-12}$ |
| `test_similarity_gauge_geometric_scale_equivariance` | with the gauge on, points $\times k$, areas $\times k^2$ and a shift leave the output unchanged ($k = 2.7$); with the default gauge the same rescale changes the output by more than $10^{-3}$ | $10^{-10}$ |
| `test_local_rotation_equivariance`, `test_local_drive_degree_one`, `test_boundary_scalar_channel_contracts`, `test_scale_conditioning_rotation_equivariance`, `test_latent_volume_tokens_contracts` | the optional feature channels (local patch integrals, boundary scalars, scale conditioning, latent volume tokens) preserve the rotation and translation contracts | $10^{-10}$ |
| `test_geo_pool_then_project_is_exact` | pooling the eight invariants over slices then projecting equals projecting then pooling, for both block types | $10^{-12}$, rtol 0 |
| `test_all_parameters_receive_gradients` (5 configurations) | every parameter receives a gradient (a requirement of distributed data parallel) | exact |
| `test_head_variants_run_under_bf16_autocast` (3 heads) | the head variants run and backpropagate under bf16 autocast with finite outputs | finite |
| `test_a35b_ablation_flags_run_and_differ`, `test_raw_coord_channel_breaks_equivariance_by_design` | the flags of the single-factor ablation at 35 training cases (@sec-mechanism) each run, give finite outputs and change the prediction; `use_relational_geo=False` stays exactly equivariant; `seed_mode="raw"` and `raw_coord_channel` break rotation covariance by more than $10^{-3}$, which is their purpose | as stated |

The rotation test uses a proper rotation ($\det Q = +1$). Under a
*reflection* the picture is asymmetric, and the tests say so
explicitly. Every invariant in the backbone is a norm or a dot product,
so scalar outputs are exactly invariant under reflections as well.
Two of the seven basis vectors, $e_\phi(\hat r,\hat n)$ and
$e_\phi(\hat r,\hat d)$, are cross products and therefore flip sign
under a mirror while their coefficients do not; the default vector head
is covariant under rotations and translations only, and
`test_parity_fix_reflection_equivariance` asserts that it is *not*
mirror-covariant. Three alternative heads are exactly O(3)-covariant
and verified as such (`parity_fix`, which gates the $e_\phi$
coefficients with the pseudoscalar $\hat r\cdot(\hat n\times\hat d)$;
`vector_basis="true5"` and `"true7"`, which drop or replace the
pseudovectors; `odd_head`, which builds parity-odd coefficients from
triple products against the slice anchors). None is the reference head;
their measured cost is collected in @sec-limits.

What is not a contract:

- **Geometric scale.** With the default constant gauge, ISLA is not
  scale-equivariant; $\lvert r_i\rvert$ is an absolute size and the
  gauge test asserts the dependence. Scale equivariance is a property of
  the `similarity_gauge=True` configuration only.
- **Reflection covariance of vectors**, as above.
- **Drive degree.** The output scales linearly with $\lvert d\rvert$ and a
  test confirms it, but the recipe feeds a unit direction, so the factor
  is one in every training and evaluation run. It is a normalization
  detail, not a physical law: a pressure coefficient is degree zero in
  the freestream speed and a dimensional pressure is degree two. ISLA has
  no drive-magnitude contract.
- **Query independence in the reference configuration.** Without the
  passive decoder, every surface point is an interacting token, so the
  prediction at a point depends on the rest of the sample. On DrivAerML
  at matched token counts the dependence is measured at 6.8% relative
  change of the prediction at a shared 5,000-cell prefix when the
  companion points change (GeoTransolver 2%). The exact contract is
  bought by the passive decoder at the price stated in @sec-mt2-design.

## Cost {#sec-mt2-cost}

**At the recipe dimensions (hidden 192, 12 layers, 256 slices) ISLA has
8.9M parameters, against GeoTransolver's 9.0M, and its peak training
memory at 10,000 tokens per case is 1.5 GB, against GeoTransolver's
4.6 GB.** That figure, and the 330 ms per training step, are for one
forward and backward pass in bf16 autocast on an RTX 4090 at batch
size 1 with the recipe's activation recompute on (below); storing the
per-slice invariants instead costs 3.5 GB at 285 ms per step. GeoTransolver's 4.6 GB is the
peak recorded during a training run on the same protocol, so it includes
optimizer state; for a 9M-parameter model that is under 0.2 GB. The
passive decoder adds four read blocks (1.1M parameters, 9.9M total; 4.9 GB
at 10,000 surface tokens plus 10,000 interior queries) and the query-token
interior configuration adds only a type embedding and a routing weight
(8.9M total; 6.0 GB and 0.28 s per step at the same 20,000 tokens, because
the per-slice geometry spans every token); neither is part of the
configuration behind the surface accuracy numbers
(results/v0_interior_resources_2026-09-07.json).

Where the memory goes, measured. Hooking every tensor autograd stores for
the backward pass and grouping by shape, at 10,000 tokens the stored
activations total 3.15 GiB, of which 76% is per-slice geometry: the
(points × slices × 3) relative-position vectors and their unit versions
(0.70 GiB), two bf16 copies of the (points × slices × 8) invariants
(0.94 GiB), and the (points × slices) distances and routing softmaxes
(0.76 GiB). The token path (hidden states, MLP activations) is the
remaining 0.75 GiB. Every geometric tensor is a cheap elementwise function of
five small inputs: the point positions, normals and drive direction
(points × 3 each) and the anchor positions and normals (slices × 3). The
recipe therefore rebuilds them inside the backward pass instead of storing
them (`geo_checkpoint`, one recompute region per layer covering the
routing bias and the pooled invariants). The forward output and every
gradient are bitwise identical to the stored-activation path under bf16
autocast; peak step memory falls from 3.23 to 1.42 GiB at 10,000 tokens
and from 6.34 to 2.78 GiB at 20,000, for a 1.16x step time
(results/isla_memory_recompute_2026-09-07.json; @sec-nb-geo-recompute).
What remains is the token path (0.75 GiB) and the (points × slices) routing
softmaxes themselves (0.5 GiB), which are the one per-slice quantity the
backward pass genuinely needs.

Where the memory goes, and why the ordering of two operations decides
it. Each slice layer holds the pairwise invariants $g_{is}$, a tensor of
shape $(B, N, S, 8)$, and uses them twice: once mapped to one routing
logit per pair, once pooled over slices with $\pi_{is}$ and mapped to a
width-96 feature per point. ISLA pools before projecting. The alternative
ordering, projecting each pair to width 96 and then pooling, would
require autograd to save the projected tensor $(B, N, S, 96)$ for the
backward pass: at $N = 10{,}000$ and $S = 256$ that is $2.5\times10^8$
elements, about 0.5 GB in bf16, per layer, or 5.9 GB across 12 layers.
The projection is affine and $\pi_{is}$ sums to one over slices, so

$$
\sum_s \pi_{is}\,(W_f\, g_{is} + b_f) \;=\; W_f \sum_s \pi_{is}\, g_{is} \;+\; b_f ,
$$

and pooling first produces the identical tensor while saving only
$(B, N, 8)$. The same identity applies in the read blocks. The contract
test checks it at $10^{-12}$ on both block types, and at the model level
the two orderings with identical weights differ by $4\times10^{-15}$ in
fp64, so checkpoints are interchangeable between them. The per-pair
projection would also cost about 2 GFLOP per layer each way, so pooling
first saves step time as well as memory. With it, the $(B, N, S, 8)$
invariants and the $(B, N, S, 3)$ separation vectors are the dominant
activations, and ISLA's memory is close to linear in tokens per case.

## Design choices and their measured cost {#sec-mt2-design}

**No single ingredient of ISLA carries its small-data accuracy; the
largest single effect measured is 16%, and the two contracts that cost
the most (density robustness and query independence) are bought by
configurations other than the one behind the accuracy results.** Each
choice below carries one headline number and a cross-reference to the
chapter that defines the measurement and reports the full readout. The
ablation numbers are ratios of a variant's pressure error to the
reference ISLA error (0.138) in the single-factor ablation at 35
HiLiftAeroML training cases under the frozen protocol of @sec-protocol,
two seeds; greater than one means the variant is worse (@sec-mechanism).

**Relational routing** is the largest single contribution. Removing the
anchor invariants from routing and read-back (`use_relational_geo=False`,
Transolver-style feature-only slicing) costs 1.16x at 35 cases
(@sec-mechanism).

**Exactness of equivariance** is a contract, not the source of the
accuracy. Appending raw coordinates to the seeds (`raw_coord_channel`)
costs 1.04x and replacing the invariant seeds by raw vectors
(`seed_mode="raw"`) gives 0.99x, so adding coordinates does not help;
every variant keeps an invariant path through the relational invariants,
so these ablations do not show that invariants are dispensable. What the
contract removes is the need for a canonical frame or a freestream
estimate at inference (@sec-pose).

**Measure weights** are a robustness property paid for in small-data
accuracy. Turning the routing weights off gives 0.88x and turning the
similarity gauge on gives 1.08x at 35 cases (@sec-mechanism). Under a
10:1 sampling-density bias on DrivAerML, ISLA *with the similarity gauge*
degrades 1.20x; that configuration is not the one behind any accuracy
number in this book and costs the 8% just stated. The constant-gauge
configuration behind every accuracy result degrades 13–14.5x, worse
than GeoTransolver's 3.6x (@sec-robustness-density).

**Query independence** costs 3.0–3.7x on DrivAerML surface pressure
(@sec-query-independence). The passive decoder reaches 0.203–0.211
against 0.0577 for the interacting reference at learning rate 1e-3, the
3.5–3.7x end of the range; the anchor-conditioned variant (`n_anchors`,
a fixed interacting core with all points decoding passively) reaches
0.209/0.221 against 0.0697/0.0671 for the 6,500-token interacting
control, the 3.0–3.3x end. The 6,500-token control is a token-count
control, not a matched-memory comparison with GeoTransolver
(@sec-mt2-cost). On the boundary→interior task the same trade is 2.6x,
and the interacting query-token configuration, which gives the contract up,
matches GeoTransolver's volume configuration on interior pressure (1.03x;
@sec-interior).

The flags that select these variants, and the remaining optional
channels, are listed once here so the results chapters can name them.

| flag (default) | what it changes |
|---|---|
| `similarity_gauge` (off) | measure-weighted centroid and RMS-radius reference length in place of the plain mean and constant `reference_length` |
| `use_measure_weights` (on) | log-measure bias in the routing softmax |
| `use_relational_geo` (on) | anchor-relational invariants in routing and read-back |
| `seed_mode` (`"invariant"`) | `"raw"` feeds $[r_i, \hat n_i, \hat d]$ instead of the five invariants |
| `raw_coord_channel` (off) | appends $r_i$ and $\hat n_i$ to the seeds |
| `query_independent`, `n_decoder_layers` (off, 4) | passive decoder with read blocks and local kernel readout |
| `n_anchors` (0) | interacting core restricted to a fixed-size anchor subset; all points decode passively |
| `interior_queries` (off) | proxy normal for off-surface queries from the anchor normals |
| `latent_volume_tokens` (off) | equivariant off-surface tokens along anchor normals; requires the passive decoder |
| `use_local_features`, `local_radii` (off) | seven measure-weighted patch-integral invariants per radius appended to the seeds |
| `n_boundary_scalars` (0) | extra per-point scalar inputs (boundary-condition traces), pass-through to the seeds |
| `scale_conditioning` (off) | appends the log raw cloud radius, deliberately breaking scale equivariance |
| `parity_fix`, `vector_basis`, `odd_head` | the O(3)-covariant vector heads of @sec-mt2-contracts |

Every flag is verified by the tests of @sec-mt2-contracts to preserve
the rotation and translation contracts unless breaking them is its
purpose, in which case the test asserts the break.
