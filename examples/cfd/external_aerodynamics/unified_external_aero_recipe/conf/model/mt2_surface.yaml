# @package _global_
# Backward-compatible name: the architecture is ISLA (Invariant Slice Attention);
# this file only inherits conf/model/isla_surface.yaml so existing launch scripts
# that pass `model=mt2_surface` keep working.
defaults:
  - isla_surface
